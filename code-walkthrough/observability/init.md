# `observability/__init__.py`

This file is intentionally empty. Its only purpose is to mark `observability` as a Python package so its modules can be imported as `agentic_rag.observability.<module>` elsewhere in the codebase.
