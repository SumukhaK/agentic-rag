# `ingestion/__init__.py`

This file is intentionally empty. Its only purpose is to mark `ingestion` as a Python package so its modules can be imported as `agentic_rag.ingestion.<module>` elsewhere in the codebase.
