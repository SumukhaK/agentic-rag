# `__init__.py`

This file is intentionally empty. Its only purpose is to mark `agentic_rag` as a Python package so its modules can be imported as `agentic_rag.<subpackage>.<module>` elsewhere in the codebase.
