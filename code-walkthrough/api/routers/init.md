# `api/routers/__init__.py`

This file is intentionally empty. Its only purpose is to mark `routers` as a Python package (a subpackage of `agentic_rag.api`) so its modules can be imported as `agentic_rag.api.routers.<module>` elsewhere in the codebase.
