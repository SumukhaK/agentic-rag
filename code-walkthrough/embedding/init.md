# `embedding/__init__.py`

This file is intentionally empty. Its only purpose is to mark `embedding` as a Python package so its modules can be imported as `agentic_rag.embedding.<module>` elsewhere in the codebase.
