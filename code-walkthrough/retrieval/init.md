# `retrieval/__init__.py`

This file is intentionally empty. Its only purpose is to mark `retrieval` as a Python package so its modules can be imported as `agentic_rag.retrieval.<module>` elsewhere in the codebase.
