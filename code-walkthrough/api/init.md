# `api/__init__.py`

This file is intentionally empty. Its only purpose is to mark `api` as a Python package (a subpackage of `agentic_rag`) so its modules can be imported as `agentic_rag.api.<module>` elsewhere in the codebase.
