# `generation/__init__.py`

This file is intentionally empty. Its only purpose is to mark `generation` as a Python package so its modules can be imported as `agentic_rag.generation.<module>` elsewhere in the codebase.
