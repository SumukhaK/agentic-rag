# `loadtest/__init__.py`

This file is intentionally empty. Its only purpose is to mark `loadtest` as a Python package so its modules can be imported as `agentic_rag.loadtest.<module>` elsewhere in the codebase.
