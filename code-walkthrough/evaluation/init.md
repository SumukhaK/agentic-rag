# `evaluation/__init__.py`

This file is intentionally empty. Its only purpose is to mark `evaluation` as a Python package so its modules can be imported as `agentic_rag.evaluation.<module>` elsewhere in the codebase.
