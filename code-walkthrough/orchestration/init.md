# `orchestration/__init__.py`

This file is intentionally empty. Its only purpose is to mark `orchestration` as a Python package so its modules can be imported as `agentic_rag.orchestration.<module>` elsewhere in the codebase.
