# `indexing/__init__.py`

This file is intentionally empty. Its only purpose is to mark `indexing` as a Python package so its modules can be imported as `agentic_rag.indexing.<module>` elsewhere in the codebase.
